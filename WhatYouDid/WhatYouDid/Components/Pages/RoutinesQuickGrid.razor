@page "/routinesquickgrid"
@using Microsoft.AspNetCore.Components.QuickGrid;
@using WhatYouDid.Data;
@inject ApplicationDbContext db


<!--<QuickGrid Items="@db.Routines" >
	<PropertyColumn Property="@(c => c.RoutineId)" Sortable="true" />
	<PropertyColumn Property="@(c => c.Name)" Sortable="true" />
</QuickGrid>
-->

@code {
	// QuickGrid<Routine> routineGrid;
	// 
	// async Task HandleSomeEvent()
	// {
	// 	await routineGrid.RefreshDataAsync();
	// }
}
